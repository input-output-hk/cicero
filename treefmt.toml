[formatter.go]
command = "gofmt"
options = ["-w"]
includes = ["*.go"]

[formatter.nix]
command = "nixpkgs-fmt"
includes = ["*.nix"]

[formatter.cue]
command = "cue"
options = ["fmt"]
includes = ["*.cue"]
